---
phase: 01-quick-wins-correcci-n-low-risk
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified: [pyproject.toml, uv.lock, README.md, docs/index.md]
autonomous: true
requirements: [DEP-01]

must_haves:
  truths:
    - "encino-orm ya no aparece en pyproject.toml ni en uv.lock"
    - "uv.lock queda podado de los 5 transitivos de encino-orm (aiomysql, aiosqlite, asyncpg, async-timeout, pymysql)"
    - "El venv local queda sincronizado (uv sync) y la suite completa sigue verde"
    - "Los docs de usuario (README.md, docs/index.md) no implican una dependencia 'companion'"
  artifacts:
    - path: "pyproject.toml"
      provides: "dependencies sin encino-orm"
      contains: "pydantic>=2"
    - path: "uv.lock"
      provides: "Lock regenerado sin encino-orm ni transitivos"
      contains: "name = \"pydantic\""
    - path: "README.md"
      provides: "Fraseo neutro del contrato de entrada"
      contains: "salida de consultas"
    - path: "docs/index.md"
      provides: "Fraseo neutro del contrato de entrada"
      contains: "salida de consultas"
  key_links:
    - from: "pyproject.toml dependencies"
      to: "uv.lock requires-dist"
      via: "regeneración con uv lock"
      pattern: "requires-dist"
---

<objective>
Eliminar la dependencia muerta `encino-orm` del paquete (DEP-01, D-10): quitar la declaración de `pyproject.toml`, regenerar `uv.lock`, podar el venv local, y limpiar las referencias documentales engañosas de README/docs. El contrato de entrada permanece `list[dict]` — no se toca código.

Purpose: `encino-orm>=0.2.1` está declarado como dependencia runtime sin un solo import en `encino_rpt/` ni `tests/` (verificado por grep). Una instalación nueva arrastra 6 paquetes innecesarios (encino-orm + aiomysql, aiosqlite, asyncpg, async-timeout, pymysql). Onda 2 a propósito: la verificación end-to-end (suite completa tras `uv sync`) debe incluir los tests de regresión nuevos de los planes 01-01/01-02, y la mutación del venv compartido no debe correr en paralelo con `uv run pytest` de otros planes.
Output: `pyproject.toml` con solo `pydantic>=2`, `uv.lock` regenerado, README/docs con fraseo neutro, suite verde.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-quick-wins-correcci-n-low-risk/01-CONTEXT.md
@.planning/phases/01-quick-wins-correcci-n-low-risk/01-RESEARCH.md
@.planning/phases/01-quick-wins-correcci-n-low-risk/01-PATTERNS.md

Decisión bloqueada implementada aquí: D-10 (eliminar `encino-orm` de `dependencies` AND limpiar referencias 'companion' engañosas en README/docs; contrato de entrada permanece `list[dict]`; regenerar `uv.lock`). Alcance documental según Research Open Question 1 / Assumption A3: README.md y docs/index.md se re-frasean; `docs/design/10-report.md` se conserva (contrato de diseño, ya dice "opcional/cero dependencias"); los snapshots GSD (`.planning/codebase/*.md`, `AGENTS.md`) NO se editan a mano — se regeneran con el próximo escaneo.

<interfaces>
<!-- Estado actual verificado por lectura directa. -->

`pyproject.toml` líneas 32-35 (a modificar):

    dependencies = [
        "encino-orm>=0.2.1",     # ← línea 33: ELIMINAR (DEP-01)
        "pydantic>=2",
    ]

`README.md` líneas 3-4 y `docs/index.md` líneas 3-4 (idénticas, a re-frasear):

    Reporteador financiero sobre `list[dict]`. Convierte filas ya materializadas
    (la salida de `fetch_all` / `fetch_many` / `paginate`) en un **árbol canónico**

`uv.lock` (no editable a mano): entrada `name = "encino-orm"` (línea ~316), `{ name = "encino-orm" }` en wheels (línea ~335), `{ name = "encino-orm", specifier = ">=0.2.1" }` en `requires-dist` de `encino-rpt` (línea ~362), y los paquetes transitivos `aiomysql`, `aiosqlite`, `asyncpg`, `async-timeout`, `pymysql`. NOTA: el paquete `paginate` en uv.lock (líneas ~621/699) es el paginador de mkdocs — NO relacionado con encino-orm; NO debe podarse.

Bloque `[project.optional-dependencies]` (excel/pdf), `[dependency-groups]` (dev/docs) y `[tool.*]`: NO se tocan.

Comando de entorno (verificado disponible): `uv` 0.8.22 con `uv lock` / `uv sync`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eliminar encino-orm de pyproject.toml y regenerar el lock</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    pyproject.toml (líneas 32-35 — bloque de dependencies actual)
    01-PATTERNS.md (sección pyproject.toml y uv.lock)
    01-RESEARCH.md (Pitfall 6 líneas 257-261, Installation líneas 100-106, Runtime State Inventory línea 220)
  </read_first>
  <action>
1. Editar `pyproject.toml`: eliminar la línea `"encino-orm>=0.2.1",` del bloque `dependencies` (línea 33). El bloque resultante debe quedar con un único elemento: `dependencies = [` seguido de `"pydantic>=2",` y `]` (sin comas colgantes extra ni elementos adicionales). NO tocar los extras (`excel`, `pdf`), los grupos dev/docs, `[tool.pytest.ini_options]`, ni `requires-python`.

2. Regenerar el lock: ejecutar `uv lock` (reescribe `uv.lock` podando `encino-orm` + los 5 transitivos y el `requires-dist` de `encino-rpt`).

3. Poda el venv local: ejecutar `uv sync`.

4. Revisar `git diff uv.lock` ANTES de continuar: el diff DEBE mostrar la eliminación únicamente de `encino-orm`, `aiomysql`, `aiosqlite`, `asyncpg`, `async-timeout`, `pymysql` (+ el `requires-dist` de `encino-rpt` y entradas `wheel`/`sdist` correlacionadas). Si el diff toca paquetes no relacionados (mkdocs, pytest, pydantic, etc.), detenerse y reportar (Pitfall 6) — no committear un lock con cambios colaterales.
  </action>
  <verify>
    <automated>grep -n "encino-orm" pyproject.toml uv.lock; test $? -eq 1</automated>
    <automated>uv run pytest -q</automated>
  </verify>
  <done>`encino-orm` sin matches en pyproject.toml y uv.lock (grep exit 1); suite completa verde tras `uv sync`; `git diff uv.lock` limitado a los 6 paquetes esperados.</done>
  <acceptance_criteria>
    - `grep -n "encino-orm" pyproject.toml uv.lock` → exit 1 (sin matches)
    - `grep -n "name = \"pydantic\"" uv.lock` → ≥ 1 match (pydantic permanece como única dependencia runtime)
    - `git diff uv.lock` NO contiene cambios en paquetes fuera de: encino-orm, aiomysql, aiosqlite, asyncpg, async-timeout, pymysql, requires-dist de encino-rpt
    - `uv run pytest -q` exit 0 (suite completa: 32 tests originales + los nuevos de los planes 01-01/01-02)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fraseo neutro del contrato de entrada en README/docs + audit</name>
  <files>README.md, docs/index.md</files>
  <read_first>
    README.md (líneas 1-5 — contexto del re-fraseo)
    docs/index.md (líneas 1-5)
    01-PATTERNS.md (sección README.md + docs/index.md, propuesta de re-fraseo línea 435)
    01-RESEARCH.md (Open Question 1 líneas 379-382, Runtime State Inventory línea 223)
  </read_first>
  <action>
Re-frasear SOLO la línea 4 de `README.md` y de `docs/index.md` (idénticas hoy):

  - Antes: `(la salida de `fetch_all` / `fetch_many` / `paginate`) en un **árbol canónico**`
  - Después: `(salida de consultas a base de datos, sin acoplarse al motor) en un **árbol canónico**`

O sea, las líneas 3-4 quedan:

    Reporteador financiero sobre `list[dict]`. Convierte filas ya materializadas
    (salida de consultas a base de datos, sin acoplarse al motor) en un **árbol canónico**

NO editar: `docs/design/10-report.md` (contrato de diseño — sus menciones de `fetch_all`/`encinorm` son ejemplos de integración y el documento ya declara "cero dependencias"), `.planning/codebase/*.md`, `AGENTS.md` (snapshots GSD regenerados por escaneo), ni `mkdocs.yml`.

Audit final: `grep -rn "encino-orm\|fetch_all\|fetch_many\|paginate" README.md docs/index.md` debe devolver 0 matches (los dos archivos editados quedan sin referencias al método de salida del ORM).
  </action>
  <verify>
    <automated>grep -n "salida de consultas a base de datos" README.md docs/index.md</automated>
    <automated>grep -n "encino-orm\|fetch_all\|fetch_many\|paginate" README.md docs/index.md; test $? -eq 1</automated>
    <automated>uv run pytest -q && uv run ruff check</automated>
  </verify>
  <done>README.md y docs/index.md usan el fraseo neutro; los dos archivos no contienen `encino-orm`/`fetch_all`/`fetch_many`/`paginate`; suite completa + ruff verdes.</done>
  <acceptance_criteria>
    - `grep -n "salida de consultas a base de datos" README.md` → 1 match
    - `grep -n "salida de consultas a base de datos" docs/index.md` → 1 match
    - `grep -n "fetch_all\|fetch_many\|paginate\|encino-orm" README.md` → exit 1, igual para `docs/index.md`
    - `docs/design/10-report.md` sin modificaciones (`git diff --stat docs/design/10-report.md` vacío)
    - `uv run pytest -q` exit 0 y `uv run ruff check` exit 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cadena de suministro del paquete (dependencias declaradas) → entorno del instalador | La declaración de dependencias runtime determina qué paquetes se instalan con `encino-rpt` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | Tampering | `pyproject.toml` dependencies | mitigate | Eliminación de la dependencia muerta: superficie de ataque reducida (6 paquetes menos en el árbol de dependencias del instalador). Verificado por grep de 0 imports (RESEARCH.md `[VERIFIED: grep]`) y por `grep encino-orm pyproject.toml uv.lock` → sin matches. |
| T-01-07 | Tampering | `uv.lock` | mitigate | `uv lock` regenera con resolución limpia; Pitfall 6 exige revisar `git diff uv.lock` para detectar cambios colaterales antes de commitear. |
| T-01-08 | Spoofing | Docs de usuario | accept | El re-fraseo neutraliza la implicación de dependencia 'companion' (D-10). `docs/design/10-report.md` se conserva por ser el contrato de diseño (Research A3); sus menciones de `fetch_all`/`encinorm` no afirman dependencia. |
| T-01-SC | Tampering | Dependencias (npm/pip/cargo) | mitigate | La fase ELIMINA un paquete (no instala ninguno). Tabla de legitimidad en RESEARCH.md: `encino-orm` REMOVED — sin gate de paquetes nuevos. |
</threat_model>

<verification>
- `grep -n "encino-orm" pyproject.toml uv.lock` → sin matches
- `git diff uv.lock` limitado a los 6 paquetes esperados (Pitfall 6)
- `uv run pytest -q && uv run ruff check` exit 0 (gate de onda: suite completa con los tests nuevos de 01-01/01-02)
- Los comandos corren en < 60 s (`uv lock`/`uv sync` pueden tardar unos segundos; la suite ~1 s + ruff ~2 s)
</verification>

<success_criteria>
- `encino-orm` eliminado de `pyproject.toml` (ROADMAP SC 5): solo `pydantic>=2` permanece como dependencia runtime
- `uv.lock` regenerado sin encino-orm ni sus 5 transitivos; una instalación nueva ya no arrastra la dependencia
- README.md/docs/index.md sin referencias engañosas al 'companion'
- Suite completa verde (32 tests originales + regresiones nuevas de los planes 01-01 y 01-02) + `ruff check` limpio
- `docs/design/10-report.md` y snapshots GSD intactos
</success_criteria>

<output>
Create `.planning/phases/01-quick-wins-correcci-n-low-risk/01-03-SUMMARY.md` when done
</output>