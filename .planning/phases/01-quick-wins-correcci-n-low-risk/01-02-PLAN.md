---
phase: 01-quick-wins-correcci-n-low-risk
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [encino_rpt/aggregation.py, tests/test_report.py]
autonomous: true
requirements: [CORR-04, CORR-05]

must_haves:
  truths:
    - "order_by(expression=...) evalúa con las funciones custom registradas vía add_function (p. ej. 'doblado(monto)' no lanza ExpressionError)"
    - "order_by(total=...) con un total inexistente lanza ValueError en español que nombra el total (no TypeError crudo)"
    - "El error de total inexistente se lanza en tiempo de run() en el primer hijo que carece del total (D-06)"
    - "El ordenamiento por total existente sigue funcionando (test_order_top_suppress intacto)"
  artifacts:
    - path: "encino_rpt/aggregation.py"
      provides: "_apply_order/_sort_key con parámetro functions y raise en total ausente"
      contains: "total de orden inexistente"
    - path: "tests/test_report.py"
      provides: "Tests de regresión CORR-04/CORR-05"
      contains: "test_order_by_missing_total_raises"
  key_links:
    - from: "aggregation.py:260 (call site)"
      to: "_apply_order"
      via: "threading de report._functions (patrón aggregation.py:102,174)"
      pattern: "_apply_order(spec, node.children, report._functions)"
    - from: "_sort_key"
      to: "evaluate"
      via: "contexto de orden con funciones (era {})"
      pattern: "evaluate(expression, getattr(child, \"_first_row\", {}), functions)"
    - from: "_sort_key (branch total)"
      to: "ValueError"
      via: "error claro en español con !r (convención aggregation.py:45)"
      pattern: "raise ValueError"
---

<objective>
Corregir los dos bugs de `order_by` en la fase C del motor de agregación: las expresiones de orden se evalúan sin las funciones custom (CORR-04) y un total inexistente produce `TypeError` crudo en `sorted()` (CORR-05).

Purpose: `_sort_key` es el único sitio de evaluación que pasa `{}` en lugar de `report._functions` (aggregation.py:300) — alinearlo con los otros 3 sitios es cirugía mínima de un call site. D-05/D-06 exigen un error ruidoso y claro que nombre el total, no fallback silencioso.
Output: `_apply_order`/`_sort_key` con threading de funciones y `raise ValueError` descriptivo; tests de regresión en `tests/test_report.py`.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-quick-wins-correcci-n-low-risk/01-CONTEXT.md
@.planning/phases/01-quick-wins-correcci-n-low-risk/01-RESEARCH.md
@.planning/phases/01-quick-wins-correcci-n-low-risk/01-PATTERNS.md

Decisiones bloqueadas implementadas aquí: D-04 (`_sort_key` evalúa con `report._functions`, NO se inyecta `TOTAL()` en el contexto de orden), D-05 (error claro en español que nombre el total, estilo `f"total de orden inexistente: {name!r}"`), D-06 (error en cuanto CUALQUIER hijo carece del total — raise en el primer hijo faltante). Sin ideas diferidas en esta fase.

<interfaces>
<!-- Contratos extraídos del código actual (aggregation.py) — el ejecutor NO necesita explorar. -->

Call site único de `_apply_order` (aggregation.py:260, dentro de `_build_instance(report, spec, key, rows, sources, registry, deferred, visible)` — `report` ya está disponible):

    node.children = _apply_order(spec, node.children)

Estado actual (líneas 277-307):

    def _apply_order(spec, children):
        if spec.order_by:
            ob = spec.order_by
            reverse = ob.get("direction") == "desc"
            children = sorted(children, key=lambda c: _sort_key(c, ob), reverse=reverse)   # ← falta functions
        if spec.suppress_zero:
            sz = spec.suppress_zero
            children = [c for c in children if not _is_zero(c, sz)]
        if spec.top_n is not None:
            children = children[: spec.top_n]
        return children

    def _sort_key(child, ob):
        total = ob.get("total")
        expression = ob.get("expression")
        column = ob.get("column")
        if total:
            for t in getattr(child, "totals", []):
                if t.name == total:
                    return t.value
            return None                                          # ← BUG CORR-05: TypeErrors en sorted()
        if expression:
            return evaluate(expression, getattr(child, "_first_row", {}), {})   # ← BUG CORR-04: pasa {}
        if column:
            if isinstance(child, Group):
                return child.key.get(column) if child.key else None
            if isinstance(child, Detail):
                return child.row.get(column)
            return None
        return 0

Patrón de threading de referencia (todo aggregation.py pasa `report._functions`): línea 102 en `_enrich` (`evaluate(f.expression, enriched, report._functions)`), línea 174 en `_compute_totals_into` (`_value_for(..., report._functions, report._aggregates)`), línea 365 y 396 (fase B / `_resolve_deferred`).

Firma del evaluador (expressions.py:54-60): `def evaluate(expr: str, row: dict, functions: dict | None = None) -> Any` — fusiona `{**_FUNCTIONS, **(functions or {})}`.

Convención de errores (aggregation.py:45): `raise ValueError(f"operador desconocido: {operator!r}")` — español + valor ofensor vía `!r`.

`Group` lleva `_first_row` (dict de la primera fila) — solo los `Group`, NO los `Detail` (Pitfall 7: el test de expresión debe ordenar hijos de tipo Group).

`Section.order_by(column=..., *, direction="asc", total=..., expression=...)` (section.py:117-138) SOLO almacena el dict en `GroupSpec.order_by` — NO tocar.

`Report.add_function(name: str, fn) -> Report` (report.py:42-53): registra en `report._functions`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir tests de regresión RED para CORR-04 y CORR-05 (order_by)</name>
  <files>tests/test_report.py</files>
  <read_first>
    tests/test_report.py (patrón `test_order_top_suppress` líneas 182-195; patrón `pytest.raises` líneas 32-38; imports líneas 1-4)
    encino_rpt/aggregation.py (líneas 277-307 — estado actual que los tests deben hacer fallar)
    01-PATTERNS.md (sección tests/test_report.py)
    01-RESEARCH.md (§Code Examples 3-4 líneas 315-348 + Pitfall 4 líneas 245-249 + Pitfall 7 líneas 263-267)
  </read_first>
  <action>Añadir SOLO tests tras `test_order_top_suppress` (línea 195), sin tocar código fuente. Ambos deben quedar en ROJO contra el código actual:

  - `test_order_by_expression_with_custom_function`: seguir EXACTAMENTE el armado de `test_order_top_suppress` (rows `{"agente", "total"}`, `rep.group("por_agente", columns="agente")`, `rep.section("por_agente").total("sum", "total", name="total_agt")`, `rep.group("global")`) pero:
    - Registrar función custom ANTES de `run()`: `rep.add_function("doblado", lambda v: v * 2)`
    - `rep.section("global").order_by(expression="doblado(total)", direction="desc")`
    - Assert del orden: `[c.key["agente"] for c in result.root.children] == ["Bob", "Cid", "Ana"]` (doblado: Ana=200, Cid=400, Bob=600)
    - NOTA (Pitfall 7): la expresión ordena hijos de tipo `Group` (llevan `_first_row`); NO usar reporte sin grupos

  - `test_order_by_missing_total_raises`: mismo armado base, pero `rep.section("global").order_by(total="total_inexistente")` (nombre NUNCA declarado) y:
    - `with pytest.raises(ValueError, match="total de orden inexistente: 'total_inexistente'"):` envolviendo `rep.run()`
    - El match cubre D-05 (nombra el total con `!r`); D-06 queda garantizado por la ubicación del raise en `_sort_key` (primer hijo evaluado por `sorted()`)

  NO usar fixtures ni conftest (convención TESTING.md); NO modificar `test_order_top_suppress` ni ningún otro test existente.
  </action>
  <verify>
    <automated>uv run pytest tests/test_report.py -k "order_by" -x; test $? -ne 0</automated>
  </verify>
  <done>Los 2 tests existen y fallan contra el código actual: `test_order_by_expression_with_custom_function` lanza `ExpressionError` (función 'doblado' no permitida con `{}`), `test_order_by_missing_total_raises` falla porque `pytest.raises(ValueError)` no captura el `TypeError` crudo (o no encuentra el mensaje).</done>
  <acceptance_criteria>
    - `grep -c "def test_order_by_expression_with_custom_function" tests/test_report.py` == 1
    - `grep -c "def test_order_by_missing_total_raises" tests/test_report.py` == 1
    - `grep -n "add_function(\"doblado\"" tests/test_report.py` → 1 match
    - `grep -n "match=\"total de orden inexistente" tests/test_report.py` → 1 match
    - El comando verify devuelve exit != 0 (RED probado)
    - `test_order_top_suppress` intacto (los `grep -c "test_" tests/test_report.py` no bajan; diff solo añade)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread de functions en _apply_order/_sort_key + raise en total ausente (CORR-04/CORR-05, D-04/D-05/D-06)</name>
  <files>encino_rpt/aggregation.py</files>
  <read_first>
    encino_rpt/aggregation.py (call site línea 260; `_apply_order` 277-287; `_sort_key` 290-307; patrón de threading 102/174/365/396; `evaluate` importado línea 7; `ValueError` convención línea 45)
    01-PATTERNS.md (sección aggregation.py, reemplazo verificado líneas 199-239)
    01-RESEARCH.md (§Code Examples 3 líneas 315-341 + Pitfall 4 líneas 245-249)
  </read_first>
  <behavior>
    - order_by(expression="doblado(total)", direction="desc") con add_function("doblado", lambda v: v*2) → hijos ordenados ["Bob", "Cid", "Ana"] (sin ExpressionError)
    - order_by(total="total_inexistente") → rep.run() lanza ValueError "total de orden inexistente: 'total_inexistente' (hijo 'por_agente')" (raise en el primer hijo evaluado que carece del total, D-06)
    - order_by(total="total_agt") existente → sigue ordenando desc ["Bob", "Cid"] (test_order_top_suppress verde)
  </behavior>
  <action>Aplicar EXACTAMENTE el reemplazo verificado (Research §Code Examples 3 / PATTERNS sección aggregation.py):

1. `_apply_order` (línea 277): añadir parámetro `functions` → `def _apply_order(spec, children, functions):`
2. Dentro de `_apply_order`, en el `sorted(...)`: `key=lambda c: _sort_key(c, ob, functions)` (era `_sort_key(c, ob)`)
3. `_sort_key` (línea 290): añadir parámetro `functions` → `def _sort_key(child, ob, functions):`
4. Branch `total` (líneas 294-298): reemplazar `return None` por:
   - `child_desc = getattr(child, "name", None) or getattr(child, "key", None)`
   - `raise ValueError(f"total de orden inexistente: {total!r} (hijo {child_desc!r})")`
   El raise DEBE vivir en el branch `total` de `_sort_key` (no pre-validación externa) para que `sorted()` evalúe el key del primer hijo faltante y propague naturalmente (Pitfall 4 / D-06).
5. Branch `expression` (línea 300): `evaluate(expression, getattr(child, "_first_row", {}), functions)` (era `{}`) — D-04: solo `functions`, NO inyectar `TOTAL()` en el dict
6. Call site (línea 260): `node.children = _apply_order(spec, node.children, report._functions)`

NO tocar: `_is_zero` (310-326), `Section.order_by`, `GroupSpec.order_by`, el branch `column`, el `return 0` final, ni los guards `_MAX_*` del evaluador. NO capturar/fallback el error (convención del proyecto: raise, no silencio — CONTEXT §code_context).
  </action>
  <verify>
    <automated>uv run pytest tests/test_report.py -k "order_by" -x</automated>
    <automated>uv run pytest tests/test_report.py -x</automated>
  </verify>
  <done>Los 2 tests nuevos pasan + los 16 tests existentes de `tests/test_report.py` (verificado por grep: 16 `def test_`) siguen verdes (incluido `test_order_top_suppress` y `test_phase_b_percentage` que usa orden natural).</done>
  <acceptance_criteria>
    - `grep -n "def _apply_order(spec, children, functions)" encino_rpt/aggregation.py` → 1 match
    - `grep -n "def _sort_key(child, ob, functions)" encino_rpt/aggregation.py` → 1 match
    - `grep -n "_apply_order(spec, node.children, report._functions)" encino_rpt/aggregation.py` → 1 match
    - `grep -n "total de orden inexistente" encino_rpt/aggregation.py` → ≥ 1 match (raise con `!r`)
    - `grep -n "evaluate(expression, getattr(child, \"_first_row\", {}), functions)" encino_rpt/aggregation.py` → 1 match (el `{}` como funciones desaparece)
    - `uv run pytest tests/test_report.py -x` exit 0
    - `uv run ruff check encino_rpt/aggregation.py` exit 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| expresiones declaradas por el desarrollador → evaluador AST | Las expressions de `order_by` se evalúan sobre la primera fila de cada grupo; el contexto de funciones es controlado por el autor del reporte, no por datos no confiables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | `_sort_key` branch expression | accept | D-04 explícito: el contexto de orden recibe SOLO `report._functions` (no `TOTAL()`). Las funciones provienen del autor del reporte (código, no datos); el evaluador conserva su whitelist AST y guards `_MAX_*` intactos (SEC-04 es fase 3). |
| T-01-05 | DoS | `sorted()` con key que lanza | accept | El raise en `_sort_key` propaga por `run()` sin catch (convención del proyecto); es el comportamiento decidido en D-05/D-06. No introduce superficie nueva de ataque: el evaluador ya aplica límites de complejidad. |
| T-01-SC | Tampering | Dependencias (npm/pip/cargo) | mitigate | Sin instalaciones en esta fase; tabla de legitimidad en RESEARCH.md (solo `encino-orm` REMOVED). Sin gate de paquetes. |
</threat_model>

<verification>
- Suite del archivo: `uv run pytest tests/test_report.py -x` (exit 0)
- Suite completa tras el plan: `uv run pytest -q && uv run ruff check` (exit 0)
- Los tests corren en < 60 s (suite total ~1 s + ruff ~2 s)
</verification>

<success_criteria>
- `order_by(expression=...)` evalúa con funciones custom (ROADMAP SC 2): `order_by(expression="doblado(total)")` ordena sin `ExpressionError` — verificado por test
- `order_by(total=...)` con total inexistente lanza error claro que nombra el total (ROADMAP SC 3): `ValueError: total de orden inexistente: 'total_inexistente' (hijo 'por_agente')` — verificado por test
- Sin cambios en `Section.order_by`/`GroupSpec.order_by`/`_is_zero` (diff limitado a `_apply_order`/`_sort_key`/call site)
- `uv run pytest -q` y `uv run ruff check` verdes al cerrar el plan
</success_criteria>

<output>
Create `.planning/phases/01-quick-wins-correcci-n-low-risk/01-02-SUMMARY.md` when done
</output>