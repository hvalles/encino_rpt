---
phase: 01-quick-wins-correcci-n-low-risk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [encino_rpt/renderers/_format.py, encino_rpt/renderers/_sanitize.py, tests/test_report_renderers.py, tests/test_security.py, docs/security.md]
autonomous: true
requirements: [CORR-02, SEC-02]

must_haves:
  truths:
    - "format_value(1234567.89, Format()) devuelve '1234567.89' (sin notación científica)"
    - "format_value con thousands=True sobre precisión completa devuelve '1,234,567.89'"
    - "percent_scale con decimals=None devuelve porcentajes limpios ('7%', '29%', '25.6%')"
    - "sanitize_csv sanea valores con espacio/BOM inicial: ' =1+1' → \"' =1+1\" (valor original verbatim)"
    - "Excel fuerza data_type='s' para valores peligrosos con espacio/BOM inicial"
  artifacts:
    - path: "encino_rpt/renderers/_format.py"
      provides: "Branch decimals=None con str() y escala percent vía Decimal"
      contains: "Decimal(str(abs(value)))"
    - path: "encino_rpt/renderers/_sanitize.py"
      provides: "is_dangerous post-recorte whitespace+BOM"
      contains: "_LEADING_TRIM"
    - path: "tests/test_report_renderers.py"
      provides: "Tests de regresión CORR-02"
      contains: "test_format_value_precision_no_sci"
    - path: "tests/test_security.py"
      provides: "Tests de regresión SEC-02"
      contains: "P5: inyección de fórmulas con espacio/BOM"
    - path: "docs/security.md"
      provides: "Descripción del sanitizer actualizada"
      contains: "espacios/BOM"
  key_links:
    - from: "sanitize_csv"
      to: "is_dangerous"
      via: "detección compartida (D-08)"
      pattern: "is_dangerous"
    - from: "write_excel_cell"
      to: "is_dangerous"
      via: "detección compartida (D-08)"
      pattern: "is_dangerous"
    - from: "format_value"
      to: "_add_thousands"
      via: "miles sobre texto de precisión completa"
      pattern: "_add_thousands"
---

<objective>
Corregir los dos bugs de utilitarios compartidos de renderizado: precisión completa en `format_value` (CORR-02) y detección robusta de inyección de fórmulas ante espacio/BOM inicial en `is_dangerous` (SEC-02).

Purpose: Los renderers emiten valores financieros con pérdida de dígitos (`1.23457e+06`) y dejan abierto un bypass del sanitizer CSV/Excel (CWE-1236, clase GHSA-xrwp-2gph-985x). Ambos fixes son de bajo riesgo, tocan una sola fuente de verdad cada uno y NO cambian la arquitectura.
Output: `format_value` corregido con tests de regresión; `is_dangerous` ampliado (fuente única D-08) con tests de regresión; `docs/security.md` actualizado.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-quick-wins-correcci-n-low-risk/01-CONTEXT.md
@.planning/phases/01-quick-wins-correcci-n-low-risk/01-RESEARCH.md
@.planning/phases/01-quick-wins-correcci-n-low-risk/01-PATTERNS.md

Decisiones bloqueadas implementadas aquí: D-01 (`str(num)` con decimals=None), D-02 (sci solo en extremos `|num|>=1e16` o `<1e-4`), D-03 (encadenado con miles/percent/símbolo/paréntesis), D-07 (`is_dangerous` escanea pasado whitespace + BOM `\ufeff`), D-08 (fuente única compartida CSV+Excel), D-09 (`sanitize_csv` prefija `'` al valor ORIGINAL). No se implementan ideas diferidas (ninguna en esta fase).

<interfaces>
<!-- Contratos extraídos del código actual — el ejecutor NO necesita explorar más. -->

Current `_format.py` (bug en la línea 29, `f"{abs(num):g}"`):

    def format_value(value, fmt) -> str:
        # value None -> "", fmt None -> str(value), kind=="date" -> strftime
        # bool -> int(value)
        if isinstance(value, (int, float, Decimal)):
            num = value
            if fmt.percent_scale:
                num = num * 100        # ← artefacto binario: 0.07*100 == 7.000000000000001
            neg = num < 0
            if fmt.decimals is not None:
                text = f"{abs(num):.{fmt.decimals}f}"
            else:
                text = f"{abs(num):g}" # ← BUG CORR-02: sci prematura
        # downstream intacto: _add_thousands / "%" / symbol / neg paren

`Format` (models.py:30-40): `kind: Literal["number","currency","percent","date"] = "number"`, `decimals: int | None = None`, `thousands: bool = False`, `symbol: str | None = None`, `symbol_position = "prefix"`, `negative: Literal["minus","paren"] = "minus"`, `percent_scale: bool = False`, `pattern: str | None = None`.

`_add_thousands` (`_format.py:42-48`): tolera sci vía try/except `(ValueError, OverflowError)` — NO tocar; verificado con precisión completa: `_add_thousands("1234567.89") == "1,234,567.89"`.

Current `_sanitize.py` (bug en `is_dangerous`, líneas 8-10):

    _DANGEROUS_PREFIXES = ("=", "+", "-", "@", "\t", "\r")

    def is_dangerous(value) -> bool:
        return isinstance(value, str) and value.startswith(_DANGEROUS_PREFIXES)

    def sanitize_csv(value):      # NO tocar (D-09 ya correcto)
        if is_dangerous(value):
            return "'" + value    # valor ORIGINAL verbatim + prefijo

    def write_excel_cell(cell, value):   # NO tocar (D-08 ya correcto)
        cell.value = value
        if is_dangerous(value):
            cell.data_type = "s"

Hechos verificados por research: `"\ufeff".isspace()` es `False` (BOM NO es whitespace para `str.lstrip()`); `str(float)` de CPython usa exactamente los umbrales sci de D-02; la regex `^[\s\ufeff]+` recorta whitespace+BOM en cualquier orden.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir tests de regresión RED para CORR-02 y SEC-02</name>
  <files>tests/test_report_renderers.py, tests/test_security.py</files>
  <read_first>
    tests/test_report_renderers.py (patrón `assert format_value(...)` en líneas 8-22)
    tests/test_security.py (patrones P1 líneas 9-27: `to_csv()` end-to-end y `importorskip("openpyxl")` + `data_type`)
    encino_rpt/renderers/_format.py (estado actual — los tests deben fallar contra él)
    encino_rpt/renderers/_sanitize.py (estado actual)
    01-PATTERNS.md (secciones tests/test_report_renderers.py y tests/test_security.py)
  </read_first>
  <action>Añadir SOLO tests (sin tocar código fuente) — deben quedar en ROJO contra el código actual:

En `tests/test_report_renderers.py`, tras `test_format_value_number` (línea 22), añadir estos tests con asserts directos (import `Format` y `format_value` ya existen, líneas 4-5):
  - `test_format_value_precision_no_sci`: `format_value(1234567.89, Format())` == `"1234567.89"`
  - `test_format_value_precision_thousands`: `format_value(1234567.89, Format(thousands=True))` == `"1,234,567.89"`
  - `test_format_value_precision_percent_scale`: `format_value(0.07, Format(kind="percent", percent_scale=True))` == `"7%"`; `0.29` → `"29%"`; `0.256` → `"25.6%"`
  - `test_format_value_precision_sci_extremes`: `format_value(1e16, Format())` == `"1e+16"` y `format_value(0.00001, Format())` == `"1e-05"` (sci aceptada SOLO en extremos, D-02)
  - `test_format_value_precision_whole_float`: `format_value(2.0, Format())` == `"2.0"` (documenta el comportamiento decidido en D-01: repr round-trip; NO "arreglar" con redondeo)
  - `test_format_value_precision_currency`: `format_value(-1234567.89, Format(kind="currency", symbol="$", decimals=None, thousands=True, negative="paren"))` == `"($1,234,567.89)"` (D-03 encadenado)

En `tests/test_security.py`:
  - Añadir import: `from encino_rpt.renderers._sanitize import is_dangerous, sanitize_csv` (precedente: test_report_renderers.py:5 importa `_format`)
  - Tras el bloque P1, añadir banner `# --- P5: inyección de fórmulas con espacio/BOM ---` (convención TESTING.md) y estos tests:
    - `test_is_dangerous_leading_space_bom`: `is_dangerous(" =1+1")` is True; `is_dangerous("\ufeff=1+1")` is True; `is_dangerous("\x0c=1+1")` is True; `is_dangerous("\ufeff\t=1+1")` is True; `is_dangerous("normal")` is False
    - `test_sanitize_csv_leading_space_bom`: `sanitize_csv(" =1+1")` == `"' =1+1"` (D-09: valor original verbatim + prefijo) y `sanitize_csv("\ufeff@evil")` == `"'\ufeff@evil"`
    - `test_excel_leading_space_bom`: `pytest.importorskip("openpyxl")`; reporte end-to-end con filas cuyos valores sean `" =1+1"` y `"\ufeff@evil"` (patrón de `test_excel_formula_injection` líneas 18-27: `rep.detail(...)`, `result.to_excel()`, assert `cell.value` conservado y `cell.data_type == "s"` en ambas celdas)

NO modificar `_DANGEROUS_PREFIXES`; NO escribir asserts sobre negativos sin prefijo (`-123` NO se toca, Pitfall 8); NO usar fixtures ni conftest (convención TESTING.md).
  </action>
  <verify>
    <automated>uv run pytest tests/test_report_renderers.py -k "precision" -x; test $? -ne 0</automated>
    <automated>uv run pytest tests/test_security.py -k "leading_space_bom" -x; test $? -ne 0</automated>
  </verify>
  <done>Los 6 tests de precisión y los 3 tests P5 existen; los comandos `-k` fallan (exit != 0) probando que los tests detectan los bugs actuales (los que sí pasan hoy —percent_scale, sci_extremes, Excel— son guards de regresión del fix, no un fallo del task).</done>
  <acceptance_criteria>
    - `grep -c "test_format_value_precision" tests/test_report_renderers.py` ≥ 6
    - `grep -c "P5: inyección de fórmulas con espacio/BOM" tests/test_security.py` ≥ 1
    - `grep -c "test_is_dangerous_leading_space_bom\|test_sanitize_csv_leading_space_bom\|test_excel_leading_space_bom" tests/test_security.py` ≥ 3
    - Los 2 comandos de verify anteriores devuelven exit != 0 (RED probado)
    - Los tests existentes (`test_format_value_currency/percent/number`, `test_csv_formula_injection`, `test_excel_formula_injection`) NO han sido modificados
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Corregir format_value — precisión completa con str() y escala percent Decimal (CORR-02, D-01/D-02/D-03)</name>
  <files>encino_rpt/renderers/_format.py</files>
  <read_first>
    encino_rpt/renderers/_format.py (estado actual, líneas 9-39; `Decimal` ya importado línea 6)
    01-PATTERNS.md (sección `_format.py`, reemplazo verificado líneas 53-67)
    01-RESEARCH.md (§Code Examples 1 líneas 281-295 + Pitfall 1 líneas 227-231)
  </read_first>
  <behavior>
    - format_value(1234567.89, Format()) → "1234567.89" (nunca "1.23457e+06")
    - format_value(1234567.89, Format(thousands=True)) → "1,234,567.89"
    - format_value(0.07, Format(kind="percent", percent_scale=True)) → "7%" (no "7.000000000000001%")
    - format_value(0.29, Format(kind="percent", percent_scale=True)) → "29%"; 0.256 → "25.6%"
    - format_value(-1234567.89, Format(kind="currency", symbol="$", decimals=None, thousands=True, negative="paren")) → "($1,234,567.89)"
    - format_value(1e16, Format()) → "1e+16"; format_value(0.00001, Format()) → "1e-05" (sci tolerada, D-02)
    - format_value(2.0, Format()) → "2.0" (repr round-trip, D-01)
  </behavior>
  <action>Reestructurar el branch numérico de `format_value` (líneas 21-29) exactamente como el reemplazo verificado de RESEARCH/PATTERNS. Punto crítico: la multiplicación `num * 100` en float produce el artefacto `7.000000000000001` — el branch `decimals=None` NO debe reutilizar `num` cuando `percent_scale` está activo; debe escalar con `Decimal`.

Estado final del branch (reemplaza las líneas 21-29 manteniendo `neg = num < 0` para el signo):
  - `if fmt.decimals is not None:` → mantener la ruta actual íntegra: `num = value`; `if fmt.percent_scale: num = num * 100`; `text = f"{abs(num):.{fmt.decimals}f}"`
  - `elif fmt.percent_scale:` → `d = (Decimal(str(abs(value))) * 100).normalize()`; `text = format(d, "f")`
  - `else:` → `text = str(abs(value))` (repr round-trip; sci solo en `|num|>=1e16` o `<1e-4` por construcción de CPython, D-02)

NO tocar el downstream (líneas 30-38: `_add_thousands`, `%`, symbol prefix/suffix, `neg` paren); NO tocar `_add_thousands` ni `excel_number_format`; NO añadir try/except para valores no-finitos (A6: fuera de alcance, los tests usan solo finitos); NO eliminar el `import Decimal` (ya presente línea 6).

Referencia del reemplazo verificado por probes (Research §Code Examples 1): `Decimal(str(abs(value))) * 100` + `format(d, "f")` produce `'7'`, `'29'`, `'25.6'` para 0.07/0.29/0.256.
  </action>
  <verify>
    <automated>uv run pytest tests/test_report_renderers.py -x</automated>
  </verify>
  <done>Los 6 tests de precisión del Task 1 pasan + los 3 tests `test_format_value_*` existentes siguen verdes (suite del archivo completa: `uv run pytest tests/test_report_renderers.py` exit 0); el guard percent (`"25.6%"` con decimals=1) no se rompe.</done>
  <acceptance_criteria>
    - `grep -n "g}" encino_rpt/renderers/_format.py` → sin matches (el formato `:g` desaparece del branch decimals=None)
    - `grep -n "Decimal(str(abs(value)))" encino_rpt/renderers/_format.py` → 1 match
    - `grep -n "text = str(abs(value))" encino_rpt/renderers/_format.py` → 1 match
    - `uv run pytest tests/test_report_renderers.py -x` exit 0 (incluye los 6 tests de precisión nuevos)
    - `uv run pytest tests/test_report_renderers.py -x -k "format_value or precision"` exit 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ampliar is_dangerous — recorte whitespace+BOM (SEC-02, D-07/D-08/D-09) + docs</name>
  <files>encino_rpt/renderers/_sanitize.py, docs/security.md</files>
  <read_first>
    encino_rpt/renderers/_sanitize.py (estado actual, 28 líneas)
    01-PATTERNS.md (sección `_sanitize.py`, reemplazo verificado líneas 105-112)
    01-RESEARCH.md (§Code Examples 2 líneas 299-313 + Pitfall 2 líneas 233-237)
    docs/security.md (sección "Protección contra inyección de fórmulas", líneas 30-41)
  </read_first>
  <behavior>
    - is_dangerous(" =1+1") → True; is_dangerous("\ufeff=1+1") → True; is_dangerous("\x0c=1+1") → True; is_dangerous("\ufeff\t=1+1") → True
    - is_dangerous("normal") → False
    - sanitize_csv(" =1+1") → "' =1+1" (valor original con espacio intacto + prefijo, D-09)
    - sanitize_csv("\ufeff@evil") → "'\ufeff@evil"
    - write_excel_cell con " =1+1"/"\ufeff@evil" fuerza cell.data_type == "s" (sin cambios en write_excel_cell, D-08)
    - is_dangerous("\t=1+1") → True (comportamiento pre-existente conservado)
  </behavior>
  <action>El cambio se limita a `is_dangerous` + un import + una constante de módulo; NO tocar `_DANGEROUS_PREFIXES`, `sanitize_csv` ni `write_excel_cell` (D-08 fuente única de verdad; ambos consumidores ya prefijan el valor original / fuerzan `data_type="s"`).

En `encino_rpt/renderers/_sanitize.py`:
  - Añadir `import re` tras `from __future__ import annotations` (orden de imports: stdlib antes del blank line, convención CONVENTIONS.md)
  - Añadir constante de módulo `_LEADING_TRIM = re.compile(r"^[\s\ufeff]+")` junto a `_DANGEROUS_PREFIXES`
  - Reemplazar el cuerpo de `is_dangerous` (líneas 8-10): `return isinstance(value, str) and _LEADING_TRIM.sub("", value).startswith(_DANGEROUS_PREFIXES)`
  - Actualizar el docstring (español): "True si Excel/Calc podría tratar `value` como fórmula tras ignorar espacios/BOM iniciales."
  - NO usar `value.lstrip()` solo (no recorta `\ufeff` — `"\ufeff".isspace()` es `False`, verificado)

En `docs/security.md`, sección "Protección contra inyección de fórmulas (Excel/CSV)" (líneas 30-41): reescribir la frase "cuyo valor empieza con `=`, `+`, `-`, `@`, tabulador o retorno de carro" para indicar que la detección ignora espacios en blanco y BOM (`\ufeff`) iniciales antes de comprobar los prefijos; mantener los bullets de CSV (prefijo `'`) y Excel (`data_type = "s"`); mencionar que la misma detección aplica a ambos formatos (fuente única). NO editar `.planning/codebase/*.md` ni `AGENTS.md` (snapshots GSD, se regeneran).
  </action>
  <verify>
    <automated>uv run pytest tests/test_security.py -x</automated>
  </verify>
  <done>Los 3 tests P5 del Task 1 pasan; los tests P1-P4 existentes siguen verdes (suite del archivo completa exit 0).</done>
  <acceptance_criteria>
    - `grep -n "_LEADING_TRIM" encino_rpt/renderers/_sanitize.py` → ≥ 2 matches (definición + uso)
    - `grep -n "value.startswith(_DANGEROUS_PREFIXES)" encino_rpt/renderers/_sanitize.py` → sin matches (chequeo de primer byte eliminado)
    - `grep -n "return \"'\" + value" encino_rpt/renderers/_sanitize.py` → 1 match (sanitize_csv NO tocado, D-09)
    - `grep -n "cell.data_type = \"s\"" encino_rpt/renderers/_sanitize.py` → 1 match (write_excel_cell NO tocado, D-08)
    - `grep -n "espacios/BOM\|espacios en blanco y BOM\|whitespace" docs/security.md` → ≥ 1 match en la sección sanitizer
    - `uv run pytest tests/test_security.py -x` exit 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| datos fuente (`list[dict]`) → renderers CSV/Excel | Valores no confiables del usuario cruzan hacia celdas de hoja de cálculo; Excel/Calc puede interpretarlos como fórmulas vivas (CWE-1236) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `sanitize_csv` / `is_dangerous` (CSV) | mitigate | Recorte `^[\s\ufeff]+` en `is_dangerous` antes del chequeo de prefijos (D-07) + prefijo `'` sobre el valor ORIGINAL (D-09, ya implementado). Cierra el bypass por espacio/BOM/control documentado en GHSA-xrwp-2gph-985x. Verificado por los tests P5 nuevos. |
| T-01-02 | Tampering | `write_excel_cell` / `is_dangerous` (Excel) | mitigate | Misma fuente única `is_dangerous` (D-08) — el fix cubre Excel sin tocar `write_excel_cell`; celda forzada a `data_type="s"`. Regresión guardada por `test_excel_leading_space_bom`. |
| T-01-03 | Spoofing | `_DANGEROUS_PREFIXES` | accept | La lista OWASP `= + - @` + `\t \r` se conserva intacta (A5: `\t`/`\r` quedan redundantes tras el recorte pero mantenerlos minimiza el diff; falsos positivos en negativos `-123` son comportamiento pre-existente, Pitfall 8). |
| T-01-SC | Tampering | Dependencias (npm/pip/cargo) | mitigate | Esta fase NO instala paquetes; no hay gate de instalación. Tabla de legitimidad presente en RESEARCH.md (solo `encino-orm` REMOVED). Sin checkpoints de paquetes. |

Nota: el fix SEC-02 es la mitigación del CWE-1236 para esta fase; cubre CSV y Excel vía el `is_dangerous` compartido (D-08), como exige el threat model del phase.
</threat_model>

<verification>
- Suite completa tras el plan: `uv run pytest -q && uv run ruff check` (exit 0)
- Los tests nuevos de precisión y sanitización corren en el archivo correspondiente en < 60 s
- Los 5 tests existentes de formato y los 4 bloques P1-P4 no se modifican
</verification>

<success_criteria>
- `format_value` conserva precisión completa con `decimals=None` (ROADMAP SC 1): `"1234567.89"`, `"1,234,567.89"`, `"($1,234,567.89)"`, percent `"7%"`/`"29%"`/`"25.6%"` verificados por tests
- El sanitizer CSV detecta espacio/BOM precediendo prefijos peligrosos (ROADMAP SC 4): `" =1+1"` y `"\x0c=1+1"` saneados; Excel fuerza texto
- `docs/security.md` describe la detección post-recorte
- `uv run pytest -q` y `uv run ruff check` verdes al cerrar el plan
</success_criteria>

<output>
Create `.planning/phases/01-quick-wins-correcci-n-low-risk/01-01-SUMMARY.md` when done
</output>