---
phase: 01-quick-wins-correcci-n-low-risk
plan: 03
subsystem: infra
tags: [encino-orm, pyproject, uv, lockfile, dependency-removal, uv-sync, docs]

# Dependency graph
requires:
  - phase: 01-quick-wins-correcci-n-low-risk
    provides: "baseline 43-test suite from waves 1a/1b (format_value precision CORR-02, sanitizer whitespace/BOM SEC-02, order_by functions/missing-total CORR-04/CORR-05) as the end-to-end regression gate after `uv sync`"
provides:
  - "Dead `encino-orm>=0.2.1` runtime dependency removed from pyproject.toml — only `pydantic>=2` remains (DEP-01/D-10); fresh installs no longer drag encino-orm + 5 transitive DB drivers"
  - "uv.lock regenerated and pruned of encino-orm, aiomysql, aiosqlite, asyncpg, async-timeout, pymysql (incl. encino-rpt requires-dist) with zero collateral lock changes (Pitfall 6 verified)"
  - "README.md / docs/index.md re-phrased to neutral input-contract language — no companion-dependency implication; input contract stays list[dict]"
affects: [phase-2-correctness, publish-ci, planning-snapshots-regeneration]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "uv lock diff audit (Pitfall 6): git diff uv.lock must remove exactly the expected packages before commit — no collateral resolution changes (mkdocs/pytest/pydantic untouched, paginate kept: mkdocs paginator, unrelated to encino-orm)"
    - "Solo pydantic>=2 como dependencia runtime del paquete — contrato del proyecto tras DEP-01"

key-files:
  created: []
  modified:
    - pyproject.toml
    - uv.lock
    - README.md
    - docs/index.md

key-decisions:
  - "D-10: encino-orm eliminado de dependencies (0 imports en encino_rpt/ y tests/, verificado por grep en research); contrato de entrada permanece list[dict] — sin cambios de código"
  - "Alcance documental A3: README.md + docs/index.md re-fraseados con fraseo neutro; docs/design/10-report.md se conserva (contrato de diseño, ya declara opcional/cero dependencias); snapshots GSD (.planning/codebase/*.md, AGENTS.md) NO se editan a mano — se regeneran con el próximo escaneo"
  - "paginate en uv.lock es el paginador de mkdocs — NO relacionado con encino-orm; se conserva"

patterns-established:
  - "Pattern DEP-01 (RESEARCH Pitfall 6): revisar git diff uv.lock tras uv lock antes de commitear; diff limitado a los 6 paquetes esperados + requires-dist de encino-rpt"

requirements-completed: [DEP-01]

# Metrics
duration: 1min
completed: 2026-09-17
---

# Phase 1 Plan 3: Eliminate dead `encino-orm` dependency + neutral docs phrasing Summary

**Dead `encino-orm>=0.2.1` runtime dependency removed from pyproject.toml — `uv.lock` regenerated and pruned of encino-orm plus its 5 transitive DB drivers (aiomysql, aiosqlite, asyncpg, async-timeout, pymysql) with zero collateral changes, local venv resynced, and the misleading ORM 'companion' phrasing neutralized in README.md/docs/index.md — full 43-test suite and ruff stay green, input contract unchanged (`list[dict]`)**

## Performance

- **Duration:** < 1 min
- **Started:** 2026-09-17T01:00:14Z
- **Completed:** 2026-09-17T01:01:08Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- DEP-01: `"encino-orm>=0.2.1",` removed from `pyproject.toml` `dependencies` — only `pydantic>=2` remains as runtime dependency. A fresh `pip install encino-rpt` / `uv build` no longer drags encino-orm + aiomysql/aiosqlite/asyncpg/async-timeout/pymysql (T-01-06 mitigated: 6 packages fewer in the installer's dependency tree).
- `uv lock` regenerated `uv.lock` pruning exactly the 6 expected package entries + the `encino-rpt` `dependencies`/`requires-dist` references. `git diff uv.lock` audited before commit (Pitfall 6): **0 insertions, 115 deletions, zero collateral changes** — mkdocs, pytest, pydantic, and the `paginate` package (mkdocs paginator, unrelated to encino-orm) untouched.
- `uv sync` pruned the local venv (removed encino-orm + 5 transitives); `uv run pytest -q` → **43 passed** (32 original + 9 from 01-01 + 2 from 01-02), `uv run ruff check` → clean. Wave 2 end-to-end gate satisfied.
- Docs: README.md:4 and docs/index.md:4 re-phrased from `(la salida de \`fetch_all\` / \`fetch_many\` / \`paginate\`)` to `(salida de consultas a base de datos, sin acoplarse al motor)` — no companion-dependency implication (D-10). Audit: both files now have 0 matches for `encino-orm|fetch_all|fetch_many|paginate`.
- `docs/design/10-report.md` verified untouched (`git diff --stat` empty); `.planning/codebase/*.md` and `AGENTS.md` not hand-edited (regenerated by the next GSD scan).

## Task Commits

Each task was committed atomically:

1. **Task 1: Remove encino-orm from pyproject.toml + regenerate lock + prune venv** - `0858169` (chore)
2. **Task 2: Neutral ORM companion phrasing in README/docs + audit** - `aa7e534` (docs)

**Plan metadata:** `docs(01-03): add plan summary` (this commit, created after per-task commits)

## Files Created/Modified

- `pyproject.toml` - `dependencies` block: removed `"encino-orm>=0.2.1",` line; `["pydantic>=2"]` remains as the only runtime dependency. Extras (excel/pdf), dev/docs groups, `[tool.pytest.ini_options]`, `requires-python` untouched.
- `uv.lock` - regenerated by `uv lock`: package entries for `encino-orm`, `aiomysql`, `aiosqlite`, `asyncpg` (incl. all platform wheels), `async-timeout`, `pymysql` removed; `encino-orm` references removed from `encino-rpt` dependencies and `requires-dist`. All other packages (incl. `paginate`, mkdocs, pytest, pydantic) intact.
- `README.md` - line 4 re-phrased to `(salida de consultas a base de datos, sin acoplarse al motor) en un **árbol canónico**`; no other changes.
- `docs/index.md` - line 4 re-phrased identically to README.md; no other changes.

## Decisions Made

- Implemented locked decision D-10 exactly as specified: remove `encino-orm` from `dependencies` AND neutralize the misleading companion phrasing in README/docs; input contract stays `list[dict]` (no runtime code touched).
- Documentation scope per Research A3 (confirmed in Open Question 1): README.md + docs/index.md re-phrased; `docs/design/10-report.md` preserved (design contract, already states "opcional/cero dependencias"); GSD snapshots NOT hand-edited.
- The `paginate` package in uv.lock was identified as the mkdocs paginator and deliberately left in place (plan note).

## Deviations from Plan

None - plan executed exactly as written.

**Total deviations:** 0 auto-fixed
**Impact on plan:** N/A — no unplanned work required.

## Issues Encountered

- `uv sync` (default groups) pruned the non-default `docs` group (mkdocs family) from the local venv alongside the intended encino-orm removal. This is standard uv default-group behavior, not a collateral lock change — `uv.lock` still resolves the docs group, and `uv run --group docs mkdocs ...` reinstalls it on demand. No action required; noted for the next executor using the local venv.
- Git emitted benign `LF will be replaced by CRLF` warnings on staged files (Windows autocrlf); the `uv.lock` diff confirmed NOT a whole-file rewrite, so the plan's LF line-ending contingency (`git checkout -- uv.lock` + `uv lock`) was unnecessary.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 1 (quick wins) is complete: all 5 requirements (CORR-02, CORR-04, CORR-05, SEC-02, DEP-01) delivered across plans 01-01/01-02/01-03; ROADMAP success criteria 1-5 all satisfiable.
- Ready for Phase 2 (Correctness Crítico: idempotent `run()`, Excel SUM range correctness, full Excel cell sanitization); the 43-test regression suite now includes precision, sanitizer, and order_by coverage to guard Phase 2 changes.
- Threat register: T-01-06 (deps tampering) and T-01-07 (uv.lock) mitigated via clean lock diff; T-01-08 (docs spoofing) accepted after neutral re-phrase; T-01-SC N/A (this phase removed a package, installed none — no package-legitimacy gate triggered).
- Outstanding: GSD snapshots (`.planning/codebase/*.md`, `AGENTS.md`) still describe `encino-orm` as a declared dependency — expected to be refreshed by the next scan, not hand-edited.

## Self-Check: PASSED

- `pyproject.toml` contains `"pydantic>=2"` and no `encino-orm` (`grep -n "encino-orm" pyproject.toml uv.lock` → exit 1).
- `uv.lock` contains `name = "pydantic"` (3 matches) and no `encino-orm`; `git diff uv.lock` confirmed 0 insertions / 115 deletions limited to the 6 expected packages + encino-rpt requires-dist.
- `README.md` and `docs/index.md` each contain exactly 1 match of `salida de consultas a base de datos`; both have 0 matches of `encino-orm|fetch_all|fetch_many|paginate`; `docs/design/10-report.md` diff empty.
- Commits `0858169` (chore) and `aa7e534` (docs) verified in `git log`.
- Full verification: `uv run pytest -q` → 43 passed (0.29s); `uv run ruff check` → All checks passed; combined exit 0, well under the 60s budget.

---
*Phase: 01-quick-wins-correcci-n-low-risk*
*Completed: 2026-09-17*