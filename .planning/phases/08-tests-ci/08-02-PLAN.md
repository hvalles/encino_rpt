---
phase: 08-tests-ci
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_report_renderers.py
  - tests/test_security.py
  - tests/test_perf_smoke.py
autonomous: true
requirements: [TEST-01, TEST-02]

must_haves:
  truths:
    - "Existen tests de regresión para serialización JSON de árboles profundos (#5) y parámetros muertos de Excel (#7)"
    - "Existen tests de seguridad para null-byte en expresiones (#9) y no-inyección en modo fórmulas (#10)"
    - "Existe tests/test_perf_smoke.py con un pivot de 50k filas que aserta corrección + wall-clock < 10s (TEST-02 smoke de rendimiento)"
  artifacts:
    - path: "tests/test_perf_smoke.py"
      provides: "Smoke test de rendimiento con entrada grande (50k filas)"
      contains: "test_pivot_50k_rows_smoke"
  key_links:
    - from: "tests/test_report_renderers.py"
      to: "encino_rpt/renderers/json.py"
      via: "result.to_json() sobre árbol >1000 profundo"
    - from: "tests/test_security.py"
      to: "encino_rpt/expressions.py"
      via: "evaluate('\\x00') — null byte"
    - from: "tests/test_perf_smoke.py"
      to: "encino_rpt/pivot.py"
      via: "build_pivot 50k filas"
---

<objective>
Añadir los tests de regresión de `TEST-01` restantes (renderers + seguridad) y el smoke test de rendimiento de `TEST-02`.

Purpose: Cubrir los gaps de renderers/seguridad de `CONCERNS.md` y materializar el cuarto gate de CI (smoke de rendimiento con entrada grande).
Output: 4 tests nuevos en 2 archivos existentes + 1 archivo nuevo `tests/test_perf_smoke.py`.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@AGENTS.md
@tests/test_report_renderers.py
@tests/test_security.py
@encino_rpt/renderers/json.py
@encino_rpt/renderers/excel.py
@encino_rpt/expressions.py
@encino_rpt/pivot.py

<interfaces>
<!-- APIs y comportamientos verificados empíricamente (2026-09-17). -->
- `result.to_json()` (models.py:202) → `JsonRenderer().render()` → `json.dumps({"schema_version":"1.0", **result.model_dump(mode="json")}, ...)`.
- Árbol `path` de 1100 segmentos → `to_json()` lanza `ValueError: Circular reference detected (depth exceeded)` (guard de pydantic-core, NO `RecursionError`).
- `evaluate("\x00", {})` → en Python 3.10 lanza `ValueError` crudo; en 3.11+ lanza `ExpressionError` (subclase de `ValueError`). `pytest.raises(ValueError)` cubre ambos.
- `result.to_excel(formulas=True)`: la celda de detalle con valor de usuario `"=1+1"` queda `data_type == "s"` (texto sano); la celda de total `"=SUM(B2)"` queda `data_type == "f"` (fórmula viva). Verificado en openpyxl: A2='=1+1'→'s', B3='=SUM(B2)'→'f'.
- `ExcelRenderer.to_excel(styles=...)` y `footer(column_position=...)` son no-ops (sin crash, sin efecto) — CONCERNS.md §Tech Debt.
- Pivot 50k filas: 50×200, `row_totals[0] == 25000`, `elapsed ≈ 0.14s` (verificado).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regresiones de renderers (#5 JSON profundo, #7 parámetros muertos Excel)</name>
  <files>tests/test_report_renderers.py</files>
  <read_first>
    - tests/test_report_renderers.py (imports 1-5, patrón `pytest.importorskip("openpyxl")`, banners `# --- JSON versionado ---` y `# --- layout ---`)
    - encino_rpt/renderers/json.py (render/to_dict)
    - encino_rpt/renderers/excel.py (`_full_row` 160-166, `_total_row` 168-185, render 27-71)
    - .planning/codebase/CONCERNS.md (§Performance: deep JSON; §Tech Debt: footer/column_position + styles dead)
  </read_first>
  <action>
    Añade 2 tests a `tests/test_report_renderers.py` bajo banner `# --- regresiones TEST-01 (renderers) ---`:

    (1) `test_deep_tree_to_json` — documenta bug. `n = 1100`; `path = ".".join(str(i) for i in range(n))`; `rows=[{"cuenta": path, "monto": 7}]`; `rep.group("cuentas", path="cuenta")`; `rep.detail("cuenta", "monto")`; `rep.group("global")`; `run()`. Aserta `with pytest.raises(ValueError): result.to_json()`. Comentario previo: `# regresión documenta bug conocido — ver CONCERNS.md §Performance (serialización JSON de árboles profundos)`. (pydantic-core lanza `ValueError: Circular reference detected (depth exceeded)`, no `RecursionError`.)

    (2) `test_excel_styles_footer_dead_params` — documenta bug. Primera línea `pytest.importorskip("openpyxl")`. `rows=[{"sku":"A","monto":100}]`; `rep.detail("sku","monto")`; `rep.group("global")`; `rep.section("global").footer("Total", column_position="monto")`; `rep.section("global").total("sum","monto")`; `run()`. Aserta: `ws = result.to_excel(styles={"bold": True})` no lanza, y `"Total" in [c.value for row in ws.iter_rows() for c in row]` (el footer se renderiza como fila completa; `column_position` y `styles` son no-op). Comentario: `# regresión documenta bug conocido — ver CONCERNS.md §Tech Debt (styles/footer muertos)`.
  </action>
  <acceptance_criteria>
    - `grep -n "def test_deep_tree_to_json\|def test_excel_styles_footer_dead_params" tests/test_report_renderers.py` devuelve 2 coincidencias.
    - `test_excel_styles_footer_dead_params` comienza con `pytest.importorskip("openpyxl")`.
    - `uv run pytest tests/test_report_renderers.py::test_deep_tree_to_json tests/test_report_renderers.py::test_excel_styles_footer_dead_params -q` → 2 passed.
    - `uv run ruff check tests/test_report_renderers.py` → exit 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_report_renderers.py::test_deep_tree_to_json tests/test_report_renderers.py::test_excel_styles_footer_dead_params -q</automated>
  </verify>
  <done>2 tests nuevos presentes y pasando; ruff check limpio.</done>
</task>

<task type="auto">
  <name>Task 2: Regresiones de seguridad (#9 null byte, #10 modo fórmulas)</name>
  <files>tests/test_security.py</files>
  <read_first>
    - tests/test_security.py (imports 1-6, patrón `pytest.importorskip("openpyxl")`, banners `# --- P1 ---` / `# --- P2 ---`)
    - encino_rpt/expressions.py (`evaluate` 54-63, `ExpressionError` 50)
    - encino_rpt/renderers/excel.py (`_write_value` 128-136, `_total_row` 168-185, `_sum_formula` 138-158)
    - .planning/codebase/CONCERNS.md (§Security: null byte; §Security Considerations: formula mode)
  </read_first>
  <action>
    Añade 2 tests a `tests/test_security.py` bajo banner `# --- regresiones TEST-01 (seguridad) ---`:

    (1) `test_expression_null_byte` — documenta bug (version-robust). Aserta `with pytest.raises(ValueError): evaluate("\x00", {})`. Comentario: `# regresión documenta bug conocido — ver CONCERNS.md §Security (null byte): Python 3.10 lanza ValueError crudo; 3.11+ lanza ExpressionError (subclase de ValueError)`. `pytest.raises(ValueError)` captura ambos (el null byte siempre es rechazado, nunca evaluado).

    (2) `test_excel_formula_mode_no_user_injection` — invariante de seguridad (pasa hoy). Primera línea `pytest.importorskip("openpyxl")`. `rows=[{"sku":"=1+1","cantidad":2}]`; `rep.detail("sku","cantidad")`; `rep.group("global")`; `rep.section("global").total("sum","cantidad")`; `run()`. Aserta sobre `ws = result.to_excel(formulas=True)`: `ws["A2"].value == "=1+1"` y `ws["A2"].data_type == "s"` (valor de usuario NUNCA es fórmula viva); y `[c.value for row in ws.iter_rows() for c in row if c.data_type == "f"] == ["=SUM(B2)"]` (solo la fórmula SUM interna, generada por índices de fila, es fórmula viva).
  </action>
  <acceptance_criteria>
    - `grep -n "def test_expression_null_byte\|def test_excel_formula_mode_no_user_injection" tests/test_security.py` devuelve 2 coincidencias.
    - `test_excel_formula_mode_no_user_injection` comienza con `pytest.importorskip("openpyxl")` y aserta `data_type == "s"` para el valor de usuario y `data_type == "f"` solo para `"=SUM(B2)"`.
    - `uv run pytest tests/test_security.py::test_expression_null_byte tests/test_security.py::test_excel_formula_mode_no_user_injection -q` → 2 passed.
    - `uv run pytest tests/test_security.py -q` → exit 0 (sin regresiones en mitigaciones existentes).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_security.py -q</automated>
  </verify>
  <done>2 tests nuevos presentes y pasando; `test_security.py` completo verde.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke test de rendimiento 50k filas (TEST-02)</name>
  <files>tests/test_perf_smoke.py</files>
  <read_first>
    - tests/test_report.py (patrón `test_pivot_single_pass` 393-415 y `test_path_group_deep_no_recursion` 418-436 — estilo de asserts concretos)
    - encino_rpt/pivot.py (build_pivot)
    - .planning/codebase/TESTING.md (§Performance/regression tests)
    - .planning/phases/08-tests-ci/08-RESEARCH.md (§Pattern 3: Perf smoke test, §Code Examples)
  </read_first>
  <action>
    Crea `tests/test_perf_smoke.py` (archivo NUEVO) con un único test:

    - Imports: `import time` y `from encino_rpt import Report` (sin `pytest.importorskip` — pivot es Python puro).
    - `def test_pivot_50k_rows_smoke():` con `n = 50_000`; `rows = [{"region": f"r{i % 50}", "product": f"p{i % 200}", "amount": i % 100} for i in range(n)]`; `rep = Report(rows)`; `rep.group("global")`; `rep.section("global").pivot("region", "product", operator="sum", value_column="amount")`.
    - Mide con `t0 = time.perf_counter()`; `result = rep.run()`; `elapsed = time.perf_counter() - t0`.
    - `pivot = result.root.children[-1]`; aserta `len(pivot.rows) == 50`, `len(pivot.columns) == 200`, `pivot.row_totals[0] == sum(i % 100 for i in range(n) if i % 50 == 0)` (== 25000), y `assert elapsed < 10.0, f"pivot 50k tardó {elapsed:.2f}s (límite 10s)"`.
    - NO uses `pytest-benchmark`, `pytest-timeout` ni `-m` markers: es un gate pass/fail con `time.perf_counter()`.
  </action>
  <acceptance_criteria>
    - `tests/test_perf_smoke.py` existe y contiene `def test_pivot_50k_rows_smoke`.
    - `uv run pytest tests/test_perf_smoke.py -q` → 1 passed (elapsed real ≈ 0.1–0.2s, muy por debajo de 10s).
    - `grep -n "time.perf_counter\|row_totals\[0\]\|elapsed < 10.0" tests/test_perf_smoke.py` devuelve las 3 referencias.
    - `uv run ruff check tests/test_perf_smoke.py` → exit 0.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_perf_smoke.py -q</automated>
  </verify>
  <done>`tests/test_perf_smoke.py` creado; 1 passed con corrección + wall-clock < 10s; ruff check limpio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → evaluador/serializador | Los tests ejercitan `evaluate()` (whitelist AST) y `to_json()` con entradas locales; no introducen datos externos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02 | Tampering | tests/test_security.py | mitigate | Los tests nuevos refuerzan el sandbox de expresiones (null byte) y la barrera de inyección Excel (modo fórmulas); no relajan ninguna aserción existente. |
| T-08-03 | DoS | tests/test_perf_smoke.py | mitigate | El smoke de 50k filas fija un tope wall-clock (10s = 10× headroom sobre los ~0.14s medidos) para detectar regresiones cuadráticas del pivot. |
| T-08-04 | Repudiation | tests/test_report_renderers.py | accept | El test de JSON profundo documenta el `ValueError` de pydantic-core sin alterar el serializador; no escribe ficheros ni ejecuta código no confiable. |
</threat_model>

<verification>
- `uv run pytest tests/test_report_renderers.py tests/test_security.py tests/test_perf_smoke.py -q` → exit 0.
- `uv run ruff check tests/test_report_renderers.py tests/test_security.py tests/test_perf_smoke.py` → exit 0.
</verification>

<success_criteria>
- `tests/test_report_renderers.py` +2 tests (`test_deep_tree_to_json`, `test_excel_styles_footer_dead_params`).
- `tests/test_security.py` +2 tests (`test_expression_null_byte`, `test_excel_formula_mode_no_user_injection`).
- `tests/test_perf_smoke.py` nuevo con `test_pivot_50k_rows_smoke` (corrección + wall-clock < 10s).
- Los 4 tests de bug conocido documentan el comportamiento con comentario `# CONCERNS.md` en español; `test_excel_formula_mode_no_user_injection` es un invariante de seguridad que pasa hoy.
</success_criteria>

<output>
Create `.planning/phases/08-tests-ci/08-02-SUMMARY.md` when done
</output>
