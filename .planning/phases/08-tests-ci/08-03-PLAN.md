---
phase: 08-tests-ci
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - pyproject.toml
  - uv.lock
  - encino_rpt/
  - tests/
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "Las secciones [tool.mypy], [tool.ruff], [tool.ruff.format], [tool.coverage.run], [tool.coverage.report] existen en pyproject.toml"
    - "mypy y pytest-cov están en [dependency-groups] dev y bloqueados en uv.lock"
    - "El código fuente y los tests quedan normalizados con ruff format (una sola vez) y `ruff format --check` pasa"
    - "ruff check y la suite siguen verdes tras la normalización"
  artifacts:
    - path: "pyproject.toml"
      provides: "Config de type-check, format, coverage"
      contains: "[tool.mypy]"
  key_links:
    - from: "pyproject.toml [tool.ruff] extend-exclude"
      to: ".planning/ docs/"
      via: "evita que ruff format --check barra los .md de planificación/docs"
    - from: "pyproject.toml [tool.coverage.report] fail_under=80"
      to: "CI quality job"
      via: "gate de cobertura"
---

<objective>
Configurar las herramientas de `TEST-02` en `pyproject.toml` (deps + secciones de config) y ejecutar la normalización one-time de `ruff format` sobre `encino_rpt` y `tests`, paso previo obligatorio al gate de formato en CI.

Purpose: `ruff format --check` FALLA hoy (40+ archivos fuente/tests sin formatear); añadir el gate de CI antes de normalizar pondría CI en rojo de inmediato (Pitfall #1 de RESEARCH.md).
Output: `pyproject.toml` con 5 secciones nuevas, `uv.lock` actualizado con mypy + pytest-cov, y `encino_rpt/` + `tests/` normalizados.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-tests-ci/08-RESEARCH.md
@.planning/phases/08-tests-ci/08-PATTERNS.md
@AGENTS.md
@pyproject.toml
@.github/workflows/ci.yml

<interfaces>
<!-- Config exacta a añadir (verificada contra docs oficiales en RESEARCH.md). -->
Secciones nuevas a añadir tras el bloque `[dependency-groups]` (línea 48-59 de pyproject.toml):

[tool.mypy]
plugins = ["pydantic.mypy"]
python_version = "3.10"
check_untyped_defs = true
warn_unused_ignores = true
warn_redundant_casts = true
no_implicit_optional = true

[tool.ruff]
target-version = "py310"
line-length = 88
extend-exclude = [".planning", "docs", "dist", "build", ".venv"]

[tool.ruff.format]
quote-style = "double"

[tool.coverage.run]
source = ["encino_rpt"]
branch = false

[tool.coverage.report]
fail_under = 80
show_missing = true
exclude_lines = ["pragma: no cover", "if TYPE_CHECKING:", "if __name__ == .__main__.:"]

Regla de config a respetar: NO añadir `select`/`ignore` en `[tool.ruff]` (conserva el lint por defecto actual). El `--cov` NO va en `[tool.pytest.ini_options].addopts` (se mantiene como flag de CI únicamente).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir deps (mypy, pytest-cov) + secciones de config</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (bloque `[dependency-groups] dev` líneas 48-54; `[tool.pytest.ini_options]` 44-46; `[project] dependencies` 32-34)
    - .planning/phases/08-tests-ci/08-RESEARCH.md (§Standard Stack, §Pattern 1/2, §Code Examples líneas 246-269)
    - AGENTS.md (constraint: pydantic es la ÚNICA dependencia de runtime)
  </read_first>
  <action>
    (1) Instala las nuevas dev-deps con `uv add --group dev "mypy>=2.3.1" "pytest-cov>=7.1.0"`. Esto añade `mypy` y `pytest-cov` al grupo `dev` y actualiza `uv.lock` (coverage.py 7.16.1 entra transitivamente). NUNCA toques `[project] dependencies` (debe seguir siendo `["pydantic>=2"]`).

    (2) Añade las 5 secciones TOML listadas en `<interfaces>` al final de `pyproject.toml` (tras el bloque `[dependency-groups]`), usando exactamente los valores indicados. Sin `select`/`ignore` en `[tool.ruff]`; sin `--cov` en `[tool.pytest.ini_options]`.

    (3) Verifica que `[project] dependencies` sigue siendo únicamente `["pydantic>=2"]` (invariante de PROJECT.md).
  </action>
  <acceptance_criteria>
    - `grep -n "mypy" pyproject.toml` y `grep -n "pytest-cov" pyproject.toml` devuelven coincidencias dentro de `[dependency-groups] dev`.
    - `grep -n "\[tool.mypy\]\|\[tool.ruff\]\|\[tool.ruff.format\]\|\[tool.coverage.run\]\|\[tool.coverage.report\]" pyproject.toml` devuelve 5 coincidencias.
    - `grep -A3 "\[project\]" pyproject.toml | grep "dependencies"` seguido de `grep -A1 "^dependencies" pyproject.toml` muestra `"pydantic>=2"` y NADA más en la lista `dependencies`.
    - `grep -n "name = \"mypy\"\|name = \"pytest-cov\"\|name = \"coverage\"" uv.lock` devuelve 3 coincidencias.
    - `uv run mypy --version` imprime `mypy 2.3.1`; `uv run pytest --help | grep -q "\-\-cov"` → exit 0 (pytest-cov instalado).
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy --version && uv run pytest --help | grep -- --cov && grep -n "name = \"mypy\"\|name = \"pytest-cov\"\|name = \"coverage\"" uv.lock</automated>
  </verify>
  <done>deps instaladas y bloqueadas; 5 secciones TOML presentes; pydantic sigue siendo la única dep de runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Normalización one-time de ruff format (scoped)</name>
  <files>encino_rpt/, tests/</files>
  <read_first>
    - .planning/phases/08-tests-ci/08-RESEARCH.md (§Common Pitfalls #1 y #2)
    - AGENTS.md (convención: comillas dobles, `snake_case`)
  </read_first>
  <action>
    (1) Ejecuta la normalización SCOPED (nunca `ruff format .`, que barrería 17 `.md` de `.planning/` y `docs/`): `uv run ruff format encino_rpt tests`. Esto reformatea ~40+ archivos fuente y de tests (solo cambios de whitespace/comillas; sin cambios de lógica).

    (2) Verifica los gates:
    - `uv run ruff format --check encino_rpt tests` → exit 0 (el gate de CI usará esta forma scoped, inmune a los `.md`).
    - `uv run ruff check` → exit 0 (el lint por defecto sigue limpio tras el reformat; la config `[tool.ruff]` no cambió reglas).
    - `uv run pytest -q` → exit 0 (la suite completa sigue verde; el reformat no rompió nada).

    (3) Inspecciona `git diff --stat` para confirmar que el cambio es SOLO formato (archivos `.py` de `encino_rpt/` y `tests/`; ningún archivo `.md` de `.planning/` o `docs/` en el diff; ningún símbolo renombrado).
  </action>
  <acceptance_criteria>
    - `uv run ruff format --check encino_rpt tests` → exit 0 (antes de esta tarea fallaba con 40+ "File would be reformatted").
    - `uv run ruff check` → exit 0.
    - `uv run pytest -q` → exit 0 (79 passed + 3 xfailed + 1 smoke = suite verde; verifica que ningún test se rompió por el reformat).
    - `git diff --name-only` no incluye ningún `*.md`; solo `*.py` bajo `encino_rpt/` y `tests/`.
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff format --check encino_rpt tests && uv run ruff check && uv run pytest -q</automated>
  </verify>
  <done>`ruff format --check` pasa; ruff check y pytest verdes; diff solo de formato en `.py`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-toolchain → repo | `uv add` introduce paquetes de PyPI al entorno de desarrollo; el formateador reescribe archivos fuente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | uv add mypy / pytest-cov | mitigate | Package Legitimacy Audit ya ejecutado (RESEARCH.md §Package Legitimacy Audit): los 5 paquetes son `[OK]` (sin `[SUS]`/`[SLOP]`); versiones bloqueadas en `uv.lock`. No requiere checkpoint humano. |
| T-08-05 | Repudiation | ruff format | mitigate | El reformat es scoped a `encino_rpt tests`; `extend-exclude` protege `.planning/` y `docs/`; verificación de `git diff --name-only` garantiza que ningún `.md` ni la lógica (solo formato) cambia. No elimina código de `_sanitize.py`/`expressions.py`. |
| T-08-06 | Repudiation | [tool.coverage.report] exclude_lines | mitigate | `exclude_lines` solo cubre `pragma: no cover`, `TYPE_CHECKING`, `__main__` (líneas no-ejecutables); NO oculta `_sanitize.py` ni `expressions.py`. `source = ["encino_rpt"]` mantiene la medición sobre la librería completa. |
</threat_model>

<verification>
- `uv run ruff format --check encino_rpt tests` → exit 0.
- `uv run ruff check` → exit 0.
- `uv run pytest -q` → exit 0.
- `uv run mypy --version` → `mypy 2.3.1`.
</verification>

<success_criteria>
- `pyproject.toml` contiene `[tool.mypy]`, `[tool.ruff]`, `[tool.ruff.format]`, `[tool.coverage.run]`, `[tool.coverage.report]`.
- `[dependency-groups] dev` incluye `mypy` y `pytest-cov`; `uv.lock` los bloquea.
- `[project] dependencies` sigue siendo `["pydantic>=2"]`.
- `ruff format --check encino_rpt tests`, `ruff check` y `pytest` salen en verde tras la normalización.
</success_criteria>

<output>
Create `.planning/phases/08-tests-ci/08-03-SUMMARY.md` when done
</output>
