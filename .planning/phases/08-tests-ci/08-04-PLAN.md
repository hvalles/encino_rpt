---
phase: 08-tests-ci
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - .github/workflows/ci.yml
  - encino_rpt/
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "`uv run mypy encino_rpt` sale limpio (exit 0) sobre la librería"
    - "ci.yml tiene dos jobs: `test` (matrix 3.10-3.13) y `quality` (singleton 3.13) con type-check + format-check + coverage gate"
    - "El gate de cobertura (`--cov-fail-under=80`), el type-check y el format-check pasan localmente, replicando exactamente lo que CI ejecutará"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI endurecido con jobs test + quality"
      contains: "quality:"
  key_links:
    - from: ".github/workflows/ci.yml quality job"
      to: "pyproject.toml [tool.mypy]/[tool.ruff]/[tool.coverage]"
      via: "uv run mypy / ruff format --check / pytest --cov"
    - from: "encino_rpt/ (type fixes)"
      to: "mypy encino_rpt"
      via: "# type: ignore[code] o anotaciones"
---

<objective>
Completar `TEST-02`: dejar `uv run mypy encino_rpt` limpio (triage de tipos), reestructurar `.github/workflows/ci.yml` en jobs `test` (matrix) + `quality` (singleton), y verificar end-to-end que los 4 gates de CI pasan localmente.

Purpose: Los checks singleton (mypy, format, coverage) no deben correr 4× (una vez por Python de la matrix); corren una sola vez en Python 3.13. El smoke de rendimiento se queda en `pytest` (rápido) para detectar regresiones específicas de versión en toda la matrix.
Output: `ci.yml` con 2 jobs + `encino_rpt/` con los ajustes de tipos necesarios para mypy limpio.
</objective>

<execution_context>
@C:/Users/Hector/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Hector/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-tests-ci/08-RESEARCH.md
@.planning/phases/08-tests-ci/08-PATTERNS.md
@.planning/phases/08-tests-ci/08-VALIDATION.md
@AGENTS.md
@.github/workflows/ci.yml
@pyproject.toml

<interfaces>
<!-- Estructura objetivo de ci.yml (job quality reusa la secuencia de pasos del job test existente). -->
jobs:
  test:   # SIN CAMBIOS (matrix 3.10/3.11/3.12/3.13; uv sync; pytest; ruff check)
  quality:  # NUEVO (singleton, Python 3.13)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v4
      - name: Set up Python 3.13
        run: uv python install 3.13
      - name: Install dependencies
        run: uv sync --all-extras --group dev
      - name: Type check
        run: uv run mypy encino_rpt
      - name: Format check
        run: uv run ruff format --check encino_rpt tests
      - name: Coverage
        run: uv run pytest --cov=encino_rpt --cov-report=term-missing --cov-fail-under=80

Decisión explícita (planner): el gate de formato usa `ruff format --check encino_rpt tests` (forma scoped) en lugar de `ruff format --check .` para ser inmune al formateo de code-blocks en `.md` de `.planning/`/`docs/` (RESEARCH.md §Alternatives). El `extend-exclude` de `[tool.ruff]` se mantiene igualmente para `ruff check`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage de mypy hasta exit 0</name>
  <files>encino_rpt/</files>
  <read_first>
    - encino_rpt/models.py (Group/PrivateAttr/model_rebuild)
    - encino_rpt/aggregation.py (accesos a privados de Report, p. ej. `report._rows`/`_datasets`/`_fields`; `_first_row`)
    - encino_rpt/report.py, encino_rpt/expressions.py, encino_rpt/pivot.py
    - .planning/phases/08-tests-ci/08-RESEARCH.md (§Pattern 2, §Common Pitfalls #3 y #4)
  </read_first>
  <action>
    (1) Ejecuta `uv run mypy encino_rpt` y captura TODOS los errores.

    (2) Triage cada error en exactamente una de estas tres salidas:
    - (a) FIX: corregir con una anotación concreta cuando sea trivial y no cambie comportamiento (p. ej. tipar un parámetro `dict[str, Any]`).
    - (b) `# type: ignore[<código>]` en la línea exacta, con el código específico de mypy (NO `# type: ignore` genérico, porque `warn_unused_ignores = true` lo detectaría como innecesario). Usar para el acceso deliberado a atributos privados y al tipado `Any` de datos de fila.
    - (c) RELAX de config SÓLO como último recurso, y únicamente añadiendo flags moderados (p. ej. `disable_error_code = [...]`), NUNCA `disallow_untyped_defs = true` (conflictúa con el tipado `Any` deliberado de filas).

    (3) CONTINGENCIA (Pitfall #4): si mypy 2.3.1 emite un error de carga del plugin (`pydantic.mypy`), ejecuta `uv add --group dev "mypy==1.20.2"` (pinar a la última 1.x) y repite el triage. Registra en el SUMMARY qué versión quedó.

    (4) Objetivo final: `uv run mypy encino_rpt` → exit 0. Los comentarios añadidos son en español y no alteran la lógica.
  </action>
  <acceptance_criteria>
    - `uv run mypy encino_rpt` → exit 0, sin errores.
    - No se añadió `disallow_untyped_defs = true` ni se cambió `[tool.mypy]` salvo por un `disable_error_code` justificado (si aplica).
    - Todo `# type: ignore` lleva código específico (p. ej. `# type: ignore[attr-defined]`); `uv run mypy encino_rpt` no reporta `warn_unused_ignores`.
    - `uv run pytest -q` → exit 0 (los cambios de tipos no rompen la suite).
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy encino_rpt && uv run pytest -q</automated>
  </verify>
  <done>mypy sale limpio sobre encino_rpt; suite verde; cambios solo de tipos/comentarios (sin alterar lógica).</done>
</task>

<task type="auto">
  <name>Task 2: Reestructurar ci.yml en jobs test + quality</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (job test existente, líneas 8-30)
    - .planning/phases/08-tests-ci/08-PATTERNS.md (§ci.yml analog, líneas 77-133)
    - .planning/phases/08-tests-ci/08-RESEARCH.md (§CI Workflow Redesign, §Code Examples líneas 302-319)
  </read_first>
  <action>
    Edita `.github/workflows/ci.yml` para:
    (1) Mantener el job `test` EXACTAMENTE como está (matrix `["3.10", "3.11", "3.12", "3.13"]`, `uv sync --all-extras --group dev`, `uv run pytest`, `uv run ruff check`).
    (2) Añadir un segundo job `quality` bajo `jobs:` (al mismo nivel que `test`, sin `needs`), con `runs-on: ubuntu-latest` y los pasos listados en `<interfaces>`: checkout@v4, setup-uv@v4, `uv python install 3.13`, `uv sync --all-extras --group dev`, `uv run mypy encino_rpt`, `uv run ruff format --check encino_rpt tests`, `uv run pytest --cov=encino_rpt --cov-report=term-missing --cov-fail-under=80`.
    (3) Usa los mismos nombres de paso y acciones versionadas (`actions/checkout@v4`, `astral-sh/setup-uv@v4`) que el job `test` existente. Indentación YAML de 2 espacios para `jobs`/`steps`, consistente con el archivo actual.
  </action>
  <acceptance_criteria>
    - `grep -n "  quality:" .github/workflows/ci.yml` devuelve 1 coincidencia; `grep -n "  test:" .github/workflows/ci.yml` devuelve 1 coincidencia.
    - `grep -c "uv run mypy encino_rpt" .github/workflows/ci.yml` == 1; `grep -c "ruff format --check encino_rpt tests" .github/workflows/ci.yml` == 1; `grep -c "cov-fail-under=80" .github/workflows/ci.yml` == 1.
    - El job `test` conserva la matrix `python-version: ["3.10", "3.11", "3.12", "3.13"]` y los pasos `uv run pytest` + `uv run ruff check`.
    - El job `quality` NO tiene `strategy.matrix` (singleton) y sí el paso `uv python install 3.13`.
  </acceptance_criteria>
  <verify>
    <automated>grep -nE "test:|quality:|mypy encino_rpt|ruff format --check|cov-fail-under|python-version:" .github/workflows/ci.yml</automated>
  </verify>
  <done>ci.yml con jobs test (matrix) + quality (singleton) y los 3 checks de calidad + coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Verificación end-to-end de los gates (replica local de CI)</name>
  <files>pyproject.toml, .github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/08-tests-ci/08-VALIDATION.md (§Sampling Rate, §Manual-Only Verifications)
    - .planning/phases/08-tests-ci/08-RESEARCH.md (§Validation Architecture)
  </read_first>
  <action>
    Ejecuta, en orden, los comandos exactos que CI ejecutará y confirma exit 0 en cada uno:
    (1) `uv run pytest -q` — suite completa (incluye `test_perf_smoke.py`).
    (2) `uv run ruff check` — lint.
    (3) `uv run mypy encino_rpt` — type check.
    (4) `uv run ruff format --check encino_rpt tests` — format gate.
    (5) `uv run pytest --cov=encino_rpt --cov-report=term-missing --cov-fail-under=80` — coverage gate; anota el % reportado (esperado ≥ 82%).
    Registra los resultados en el SUMMARY. La ejecución real de GitHub Actions en push es la ÚNICA verificación manual (VALIDATION.md §Manual-Only); no hay nada que el agente deba esperar del usuario para este plan.
  </action>
  <acceptance_criteria>
    - `uv run pytest -q` → exit 0.
    - `uv run ruff check` → exit 0.
    - `uv run mypy encino_rpt` → exit 0.
    - `uv run ruff format --check encino_rpt tests` → exit 0.
    - `uv run pytest --cov=encino_rpt --cov-report=term-missing --cov-fail-under=80` → exit 0 y el reporte muestra `TOTAL ... 82%` o superior (nunca por debajo de 80).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q && uv run ruff check && uv run mypy encino_rpt && uv run ruff format --check encino_rpt tests && uv run pytest --cov=encino_rpt --cov-report=term-missing --cov-fail-under=80</automated>
  </verify>
  <done>Todos los gates de CI pasan localmente: pytest, ruff check, mypy, ruff format --check y coverage ≥ 80%.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → repo | Los jobs de GitHub Actions ejecutan `uv sync` (instala dev-deps) y `pytest`/`mypy`/`ruff` sobre el código. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-SC | Tampering | uv sync en CI | mitigate | Dependencias bloqueadas en `uv.lock` (commit al repo); Package Legitimacy Audit `[OK]` en RESEARCH.md; versiones de acciones pineadas (`checkout@v4`, `setup-uv@v4`). |
| T-08-07 | Information Disclosure | secrets en CI | accept | El workflow no introduce secretos nuevos; no hay `env:` con tokens; `uv sync` no usa credenciales. |
| T-08-08 | Repudiation | mypy triage | mitigate | Los `# type: ignore[code]` solo silencian errores específicos, no debilitan el sandbox de `expressions.py` ni `_sanitize.py`; `warn_unused_ignores = true` impide ignores superfluos. |
</threat_model>

<verification>
- `uv run mypy encino_rpt` → exit 0.
- `uv run pytest --cov=encino_rpt --cov-report=term-missing --cov-fail-under=80` → exit 0.
- `uv run ruff format --check encino_rpt tests` → exit 0.
- `.github/workflows/ci.yml` contiene jobs `test` + `quality`.
</verification>

<success_criteria>
- mypy limpio sobre `encino_rpt` (exit 0), con triage documentado (fix/ignore/relax) y, si aplica, la versión mypy elegida registrada.
- `ci.yml` reestructurado: job `test` (matrix) sin cambios + job `quality` (3.13) con type-check + format-check + coverage.
- Los 5 comandos de verificación pasan localmente, replicando el pipeline CI (TEST-02 "CI pasa end-to-end").
</success_criteria>

<output>
Create `.planning/phases/08-tests-ci/08-04-SUMMARY.md` when done
</output>
