---
phase: 11-engine-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [encino_rpt/aggregation.py, encino_rpt/report.py, encino_rpt/pivot.py, encino_rpt/renderers/json.py, tests/test_report.py, tests/test_report_renderers.py, tests/test_security.py]
autonomous: true
requirements: [CORR-08, CORR-09, CORR-10, CORR-11, CORR-12, CORR-13, CORR-14]

must_haves:
  truths:
    - "chart/pivot con expresión inválida lanzan AggregationError con contexto de grupo (no ExpressionError/TypeError crudo)"
    - "suppress_zero y agrupación con columna inexistente / valor no hashable lanzan ValueError claro (no supresión silenciosa ni TypeError)"
    - "total nombrado con valor None no rompe el registro (0 + None)"
    - "los tests xfail existentes pasan a asserts verdes al corregir el bug correspondiente"
  artifacts:
    - path: "encino_rpt/aggregation.py"
      provides: "fixes CORR-09/10/11/12"
    - path: "tests/test_report.py"
      provides: "regresiones verdes (xfail -> assert)"
  key_links:
    - from: "encino_rpt/aggregation.py"
      to: "tests/test_report.py"
      via: "convierte xfail en assert"
      pattern: "pytest.mark.xfail"
---

<objective>
Corregir 7 bugs de correctitud del motor: fail-loudly en `suppress_zero`/no-hashable, contexto en chart/pivot, guard de `None` en el registro, semántica documentada de `count`, `detail(source=)` funcional, y serialización profunda sin `RecursionError`.
</objective>

<tasks>

<task id="11-01-01" type="execute">
<read_first>
- encino_rpt/aggregation.py (_wrap :34-43, chart/pivot :353-368, _is_zero :428)
- encino_rpt/aggregation.py (_partition :178-189)
- encino_rpt/pivot.py (buckets.setdefault)
- tests/test_report.py (xfail existentes)
</read_first>
<action>
1. **CORR-10**: envolver `build_chart(cs, ...)` en `_wrap(f"gráfico (grupo {spec.name!r})", build_chart, ...)` y `build_pivot(ps, ...)` en `_wrap(f"pivote (grupo {spec.name!r})", build_pivot, ...)` (agregación.py:353-368).
2. **CORR-11**: `_is_zero` (aggregation.py:428) — si la columna no está en el `key` (Group) o `row` (Detail), lanzar `ValueError(f"columna de suppress_zero inexistente: {column!r}")` en vez de devolver `True` (mismo patrón que `_sort_key`).
3. **CORR-12**: `_partition` (aggregation.py:184) — al construir `key = tuple(r.get(c) ...)`, capturar `TypeError: unhashable` y relanzar `ValueError(f"valor no hashable en la columna de agrupación {c!r}: {valor!r}")`. En `pivot.py`, igual para `buckets.setdefault((rv, cv), [])`.
</action>
<acceptance_criteria>
- `uv run pytest tests/test_report.py -q` → los xfail de chart/pivot, suppress_zero y no-hashable pasan a verdes (o el test correspondiente lanza el `ValueError` esperado).
- `uv run ruff check` y `uv run mypy encino_rpt` limpios.
</acceptance_criteria>
</task>

<task id="11-01-02" type="execute">
<read_first>
- encino_rpt/aggregation.py (_compute_totals_into :214-235)
</read_first>
<action>
**CORR-09**: en `_compute_totals_into` (aggregation.py:235), el acumulado `registry[key] = registry.get(key, 0) + val` rompe si `val is None`. Cambiar a `registry[key] = registry.get(key, 0) + (val if val is not None else 0)`. Documentar en comentario español: un grupo vacío (total `None`) contribuye `0` al acumulado cruzado.
</action>
<acceptance_criteria>
- `uv run pytest tests/test_report.py -q` → `test_named_total_none_values` (xfail) pasa a verde.
- `uv run mypy encino_rpt` limpio.
</acceptance_criteria>
</task>

<task id="11-01-03" type="execute">
<read_first>
- encino_rpt/aggregation.py (_value_for :71-88)
- tests/test_report.py (test_count_expression_semantics)
</read_first>
<action>
**CORR-13**: documentar y fijar la semántica de `count` con `expression=`. Decisión recomendada: `count` con `expression=` cuenta las filas cuya expresión es truthy (conditional count); `count` sin expresión cuenta filas (`len(rows)`). Añadir docstring a `_value_for` y un test que fije ambas semánticas (el test existente `test_count_expression_semantics` pasa a assert explícito, no assert-de-comportamiento-actual).
</action>
<acceptance_criteria>
- `_value_for` tiene docstring que distingue `count` (rows) vs `count`+expression (truthy).
- `uv run pytest tests/test_report.py -q` verde.
</acceptance_criteria>
</task>

<task id="11-01-04" type="execute">
<read_first>
- encino_rpt/report.py (detail :330-341)
- encino_rpt/aggregation.py (build :543-551, _build_instance detalle :340-344)
</read_first>
<action>
**CORR-08** (decisión recomendada — opción b): `detail(source=X)` aplica solo a nivel raíz (sin grupos). Implementar: `Report.detail` almacena `self._detail_source = source`. En `build()`/`_build_instance`, si `report._detail_source` no es `None` y el reporte tiene grupos (children_map no vacío), lanzar `ValueError("`detail(source=...)` no es compatible con grupos; usa `group(..., source=...)`")`. Si no hay grupos, construir el detalle raíz desde `sources[detail_source]` (con validación up-front de que el source existe — ya la hay en `_validate`).
</action>
<acceptance_criteria>
- `Report.read(...)`/`Report(rows)` con `detail("col", source="otro")` + `add_dataset("otro", ...)` sin grupos produce detalle del dataset `otro`.
- Con grupos + `detail(source=...)` lanza `ValueError` claro.
- `uv run pytest -q` verde (test xfail `test_detail_source_ignored` pasa a assert verde o a `pytest.raises`).
</acceptance_criteria>
</task>

<task id="11-01-05" type="execute">
<read_first>
- encino_rpt/renderers/json.py (JsonRenderer.to_dict)
- encino_rpt/models.py (ReportResult.to_json)
- tests/test_report_renderers.py (test_deep_path_renders_iteratively)
</read_first>
<action>
**CORR-14** (decisión recomendada — error claro): en `JsonRenderer.to_dict`/`to_json`, envolver `model_dump(mode="json")` en try/except `RecursionError` (y `ValueError` de pydantic por profundidad) y relanzar `ValueError("la jerarquía es demasiado profunda para serializar a JSON; considera un reporte más plano")`. Documentar el límite en el docstring de `to_json` y en `docs/guide.md`.
</action>
<acceptance_criteria>
- `result.to_json()` sobre un árbol de >1000 de profundidad lanza `ValueError` claro, no `RecursionError`.
- `uv run pytest -q` verde; `uv run mypy encino_rpt` limpio.
</acceptance_criteria>
</task>

</tasks>

<threat_model>
- T-11-01 (mitigado): `suppress_zero`/agrupación con input hostil lanzan `ValueError` claro (no ejecución arbitraria).
- T-11-02 (mitigado): serialización profunda no expone traceback de `RecursionError` (error controlado).
- T-11-03 (info): sin superficie de seguridad nueva (solo guards y contexto de error).
</threat_model>

<verification>
- `uv run pytest -q`
- `uv run ruff check`
- `uv run mypy encino_rpt`
- `uv run ruff format --check encino_rpt tests`
</verification>
